<%= form_with model: @post, local: true do |form| %>
  <div>
    <div>
      <%= form.text_field :title, placeholder:"タイトル" %>
    </div>
    <div>
      <%= form.text_field :tag_list, placeholder:"関連するタグを1つ以上5つ以内で入力（例:ベース）"%>
    </div>
    <div>
      <div>
      <%= form.text_area :text, placeholder:"音楽の知識や技術を共有しよう" %>
      </div>
    </div>
  </div>
  <div>
    <%= form.submit "投稿する"%>
  </div>
<% end %>